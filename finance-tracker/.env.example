# Скопируйте в .env и подставьте свои значения.
# Выбор хранилища для CLI (main.py): json (по умолчанию) или postgres
STORAGE=json

# Настройки PostgreSQL (нужны только при STORAGE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finance_tracker
DB_USER=postgres
DB_PASSWORD=ваш_пароль
